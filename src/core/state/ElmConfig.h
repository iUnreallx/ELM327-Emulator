#pragma once

struct ElmConfig {
    bool echoEnabled = true; // Отвечает за эхо
    bool appendPrompt = true; // добавлять ли знак > в конце ответа
    bool spacesEnabled = true; // отвечает за пробелы в hex ответах
};
